import { Vocedirettificaconvalore } from './vocedirettificaconvalore';

describe('Vocedirettificaconvalore', () => {
  it('should create an instance', () => {
    expect(new Vocedirettificaconvalore()).toBeTruthy();
  });
});

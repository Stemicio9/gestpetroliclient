import { Puntovenditaconcliente } from './puntovenditaconcliente';

describe('Puntovenditaconcliente', () => {
  it('should create an instance', () => {
    expect(new Puntovenditaconcliente()).toBeTruthy();
  });
});
